import { Organizer } from "../../modules/organizer/domain/organizer.entity";

export const unittestOrganizers = {
    alice: new Organizer({
        id: "usr-001",
    }),
    john: new Organizer({
        id: "usr-002",
    })
}